# ============================================================
# 報告テンプレート (Report Template)
# ============================================================
# Generated by: yaml-template-generator
# Project: multi-agent-GuP
#
# 用途: 乗組員→副中隊長→中隊長への報告
#
# 使用方法:
#   1. このテンプレートをコピー
#   2. {{PLACEHOLDER}} を実際の値に置換
#   3. 報告先の queue ディレクトリに配置
#   4. send-keys で通知
#
# 配置先:
#   - 乗組員→副中隊長: queue/platoon{{N}}/reports/{{WORKER_ID}}_report.yaml
# ============================================================

worker_id: "{{WORKER_ID}}"
task_id: "{{TASK_ID}}"
timestamp: "{{TIMESTAMP}}"
status: "{{STATUS}}"

result:
  summary: |
    {{SUMMARY}}

  files_modified:
    - path: "{{FILE_PATH_1}}"
      action: created

  notes: |
    {{NOTES}}

skill_candidate:
  found: false
